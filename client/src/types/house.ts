export type House = {
  id: number;
  price: number;
  address: string;
  city: string;
  zipCode: string;
  country: string;
  description: string;
  photo: string;
};
export interface Logo {
  // Define properties for the logo component
}
